{
  "name": "web2-final",
  "private": true,
  "scripts": {
    "start": "cd backend && npm start",
    "dev": "cd backend && npm run dev",
    "frontend": "npx live-server frontend --port=5500 --host=127.0.0.1"
  }
}
